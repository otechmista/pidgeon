# Pidgeon local config (optional overrides for dev)
# OAuth client IDs and AI keys are normally set inside the app (encrypted vault).
# Do not commit real secrets.

# GOOGLE_CLIENT_ID=
# MICROSOFT_CLIENT_ID=
# Optional default AI for development only:
# PIDGEON_AI_CLOUD_BASE_URL=https://api.openai.com/v1
# PIDGEON_AI_CLOUD_API_KEY=
# PIDGEON_AI_LOCAL_BASE_URL=http://127.0.0.1:11434/v1
